	component ISSP is
		port (
			probe  : in  std_logic_vector(1 downto 0) := (others => 'X'); -- probe
			source : out std_logic_vector(1 downto 0)                     -- source
		);
	end component ISSP;

